# Preserve generated PDFs and figures byte-for-byte on every platform.
*.pdf binary
*.png binary

# Keep source hashes stable across Windows and Unix checkouts.
*.ipynb text eol=lf
*.py text eol=lf
*.tex text eol=lf

# Historical source excerpts intentionally retain their original whitespace.
comparison/*-source.diff -whitespace
